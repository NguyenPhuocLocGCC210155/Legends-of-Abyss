using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IBossAler 
{
    // Start is called before the first frame update
    void AlerBossName(GameObject obj);
}
